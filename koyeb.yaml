type: web
name: safeshell-backend
env:
  PORT: 8000
  FIREBASE_SERVICE_ACCOUNT_BASE64: "" # Set in dashboard
routes:
  - path: /
    port: 8000
regions:
  - sin
instance_types:
  - free
build:
  docker:
    dockerfile: Dockerfile
    context: .
