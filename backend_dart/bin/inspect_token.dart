


void main() {
  // We can't easily create a DecodedIdToken without a real app/token,
  // but we can look at the class definition if we were in an IDE.
  // Since we are not, I'll try to 'blindly' find it via dynamic.
}
